<Window x:Class="StampService.AdminGUI.Views.SecretDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Secret Details" 
  Height="600" Width="600"
   WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid>
    <Grid.RowDefinitions>
     <RowDefinition Height="Auto"/>
         <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
   </Grid.RowDefinitions>

     <!-- Header -->
  <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16">
    <StackPanel Orientation="Horizontal">
          <materialDesign:PackIcon Kind="InformationOutline" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
      <TextBlock Text="Secret Details" FontSize="18" VerticalAlignment="Center" FontWeight="Medium"/>
      </StackPanel>
        </materialDesign:ColorZone>

        <!-- Content -->
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
   <StackPanel Margin="24">
     
 <!-- Basic Info -->
   <materialDesign:Card Padding="16" Margin="0,0,0,16">
   <Grid>
 <Grid.ColumnDefinitions>
       <ColumnDefinition Width="120"/>
       <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
   <RowDefinition Height="Auto"/>
  <RowDefinition Height="Auto"/>
       <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="SemiBold" Margin="0,0,0,8"/>
  <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SecretNameText" TextWrapping="Wrap" Margin="0,0,0,8"/>

      <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" FontWeight="SemiBold" Margin="0,0,0,8"/>
       <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TypeText" Margin="0,0,0,8"/>

   <TextBlock Grid.Row="2" Grid.Column="0" Text="Created:" FontWeight="SemiBold" Margin="0,0,0,8"/>
    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="CreatedAtText" Margin="0,0,0,8"/>

    <TextBlock Grid.Row="3" Grid.Column="0" Text="Network:" FontWeight="SemiBold" Margin="0,0,0,8"/>
       <TextBlock Grid.Row="3" Grid.Column="1" x:Name="NetworkText" Margin="0,0,0,8"/>

   <TextBlock Grid.Row="4" Grid.Column="0" Text="Address:" FontWeight="SemiBold"/>
       <TextBlock Grid.Row="4" Grid.Column="1" x:Name="AddressText" TextWrapping="Wrap"/>
          </Grid>
       </materialDesign:Card>

     <!-- Secret Value -->
<materialDesign:Card Padding="16" Margin="0,0,0,16" Background="{DynamicResource MaterialDesignDivider}">
    <StackPanel>
     <TextBlock Text="Secret Value" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
     
   <TextBox x:Name="ValueTextBox"
       IsReadOnly="True"
    TextWrapping="Wrap"
    MinHeight="80"
    Style="{StaticResource MaterialDesignOutlinedTextBox}"
FontFamily="Consolas"
   FontSize="12"
       Margin="0,0,0,12"/>
 
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
    <Button x:Name="RevealButton"
    Content="Reveal Secret"
   Click="RevealButton_Click"
 Style="{StaticResource MaterialDesignRaisedButton}"
  Margin="0,0,8,0"/>
   <Button Content="Copy"
         Click="CopyButton_Click"
    Style="{StaticResource MaterialDesignOutlinedButton}"
      Margin="0,0,8,0"/>
    <Button Content="Export"
   Click="ExportButton_Click"
  Style="{StaticResource MaterialDesignOutlinedButton}"/>
    </StackPanel>
    </StackPanel>
     </materialDesign:Card>

        <!-- Metadata -->
   <materialDesign:Card Padding="16">
     <StackPanel>
        <TextBlock Text="Metadata" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
   
   <ListBox x:Name="MetadataList" 
BorderThickness="0"
       MaxHeight="200">
      <!-- Items added programmatically -->
   </ListBox>
   </StackPanel>
     </materialDesign:Card>

            </StackPanel>
</ScrollViewer>

   <!-- Footer -->
 <materialDesign:ColorZone Grid.Row="2" Mode="PrimaryLight" Padding="12">
       <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
    <Button Content="Close" 
      Click="CloseButton_Click"
       Style="{StaticResource MaterialDesignRaisedButton}"/>
   </StackPanel>
 </materialDesign:ColorZone>
    </Grid>
</Window>
